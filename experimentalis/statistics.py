"""
The statistics module is a simple little collection of statistics-adjacent
functions that aren't already explicitly handled with NumPy or SciPy or
anything else. As most statistics subroutines are already baked into these
standard libraries, this module is considerably small and usecase-dependent.
"""

from .data import Dataset
from .plotting import GraphingOptions


